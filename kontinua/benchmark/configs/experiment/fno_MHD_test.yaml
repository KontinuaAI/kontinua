# @package _global_
defaults:
  - /data: MHD
  - /model: fno
  - /lr_scheduler: cosine_with_warmup

trainer:
  loss_fn:
    _target_: kontinua.benchmark.metrics.MSE
  formatter: channels_first_default

name: fno
